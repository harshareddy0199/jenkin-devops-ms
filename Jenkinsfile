pipeline {
	agent any
	stages {
	    stage('Build') {
		    steps{
				echo "Build"
	        }
		}	
	    stage('TEST') {
		    steps{
				echo "TEST"
	        }
		}
		stage('QA') {
		    steps{
				echo "INTEGRATION TEST"
	        }
	    }	
	}
}
